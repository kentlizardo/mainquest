providers = ['node']

[variables]
  NODE_ENV = 'production'

[phases.setup]
  nixPkgs = ['nodejs', 'yarn']

[phases.install]
  cmd = ['yarn install']

[phases.build]
  dependsOn = ['install']
  cmds = ['turbo build --filter=native --filter=./packages/*', 'cd native']

[phases.caddy]
  dependsOn = ['build']
  nixpkgsArchive = 'ba913eda2df8eb72147259189d55932012df6301' # Caddy v2.8.4
  nixPkgs = ['caddy']

[phases.fmt]
  dependsOn = ['caddy']
  cmds = ['caddy fmt --overwrite Caddyfile']

[start]
    cmd = 'exec caddy run --config Caddyfile --adapter caddyfile 2>&1'
