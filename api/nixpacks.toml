providers = ['node']

[variables]
  NODE_ENV = 'production'

[phases.setup]
  nixPkgs = ['nodejs', 'yarn']

[phases.install]
  cmds = ['yarn']

[phases.build]
  dependsOn = ['install']
  cmds = ['yarn build']

[start]
  cmd = 'yarn start'