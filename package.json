{
  "name": "mainquest",
  "packageManager": "yarn@4.9.1",
  "workspaces": [
    "api",
    "native",
    "packages/*"
  ],
  "engines": {
    "npm": "please-use-yarn",
    "pnpm": "please-use-yarn"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "lint:fix": "turbo lint -- --fix"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
